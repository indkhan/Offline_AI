name: offline_ai
description: Offline AI chat app for Android. Local GGUF inference with no network during generation.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_bloc: ^9.1.1
  equatable: ^2.0.5
  dio: ^5.9.0
  path_provider: ^2.1.5
  path: ^1.9.1
  sqflite: ^2.4.2
  shared_preferences: ^2.5.3
  flutter_markdown: ^0.7.7+1
  uuid: ^4.5.1
  llama_flutter_android: ^0.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
